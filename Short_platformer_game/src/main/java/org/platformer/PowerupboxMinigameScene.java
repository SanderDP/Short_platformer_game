package org.platformer;

import javafx.scene.Parent;
import javafx.scene.SubScene;

public class PowerupboxMinigameScene extends SubScene {

    public PowerupboxMinigameScene(Parent parent, double v, double v1) {
        super(parent, v, v1);
    }
}
